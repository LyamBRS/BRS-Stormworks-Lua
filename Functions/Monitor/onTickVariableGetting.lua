    -- [BRS] - Requires the import of monitor variables in your main file.
    -- [BRS] - Monitor touch inputs.
    g_pressing = input.getBool(1)
    g_pressingCoordsX = input.getNumber(3)
    g_pressingCoordsY = input.getNumber(4)